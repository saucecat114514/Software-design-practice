# 00 · 项目总作战计划（主控文档 / 上下文恢复锚点）

> **这份文档是干什么的？**
> 这是《高级软件设计实践》实训项目的**唯一主控文档**。它同时服务两个目的：
> 1. **提醒你**：每天该干什么、交付什么、对应哪份规范、达标线在哪。
> 2. **提醒我（AI）**：当我的上下文被截断/清空后，**先读这一份**，就能在 2 分钟内恢复对整个项目的全部认知，不必重新通读所有 docx 和规范。
>
> 维护约定：每完成一个里程碑，回到 §9 进度看板打勾，并在 §10 记一笔。日期/版本一律写绝对值。
>
> 创建日期：2026-06-15　|　计划周期：2026-06-15（周一）→ 2026-07-10（周五），共 20 个工作日 ≈ 一个月。

---

## 1. 项目一句话定义

- **课程**：高级软件设计实践（软件工程专业大三实习实训，4 周 / 32 学时 / 2 学分）。
- **主线**：**AI 赋能的全流程软件工程实践** —— 不是「用 AI 辅助旧流程」，而是「以 AI 为协作伙伴，按 AI 时代方法论走完整闭环」。
- **选定案例**：**教育培训机构教务收费管理系统**（10 个候选项目中已锁定的这一个，需求素材见根目录 `教育培训机构教务收费管理系统需求文档.docx`）。
- **完整闭环**：需求获取 → 需求分析 → 需求规格说明 → 需求验证确认 → 架构选型 → 约束设计 → AI 代码生成(v1) → 需求变更管理 → 代码变更维护(v2) → 逆向校验 → 设计资产归档。
- **工作目录**：`D:\Project`（整个项目唯一根目录，所有产物都放这里）。**远程仓库**：https://github.com/saucecat114514/Software-design-practice （GitHub 用户名 saucecat114514）。
- **两阶段考核**：第 1–2 周成果 = **期中考核（需求工程）**；第 3–4 周成果 = **期末考核（软件设计与体系结构）**。

---

## 2. 关键事实速查表（Cheat Sheet）—— 上下文不足时优先读这一节

### 2.1 五类涉众（由教师 5000 平台 agent 扮演；我方每涉众配 1 个 A1 需求获取 agent 与之对话）
| 涉众 | 核心关注 | 需求文档中的模块数 |
|---|---|---|
| **课程顾问** | 咨询登记、课程推荐、试听、报名、优惠、缴费引导、客户跟进、转化 | 6 |
| **教务老师** | 排课、课表冲突、考勤、消课、请假、调课、补课、转班、课时统计 | 6 |
| **财务人员** | 收款、退款、预收款、收入确认、欠费、续费、发票、对账、工资、报表 | 8 |
| **校长** | 经营看板、营收、招生、续费/流失/满班/课消率、资金安全、多校区、审批、移动端 | 7 |
| **系统管理员** | 账号、角色、权限、校区数据隔离、配置、云部署、性能、备份、支付接口、日志审计、安全 | 8 |

（各涉众的详细模块清单见 §7，用于建模与 SRS 范围界定。）

### 2.2 七类 AI 智能体（需求工程阶段流水线）
`A1 获取 → A2 分析 → A3 建模 → A4 SRS生成 → A5 验证 → CCB 审批(人工) → A6 基线创立`，内嵌反馈回路（A2/A5/CCB 发现问题则回退上游）。

| 编号 | 智能体 | 职责 | 知识库写入 |
|---|---|---|---|
| A1 | 需求获取智能体（每涉众1个，共5个）⚠️ | **与涉众对话、获取并记录需求的「采访方」**（不是扮演涉众！涉众侧由教师 5000 平台的 5 个角色 agent 扮演） | `raw/notes/` 原始需求记录 |
| A2 | 需求分析智能体 | 检测 模糊/不一致/矛盾/冲突 | 问题清单、回退记录 |
| A3 | 建模智能体 | 生成 UML 用例图/活动图/时序图（PlantUML） | `wiki/summaries/` |
| A4 | 需求文档智能体 | 生成 IEEE 830 标准 SRS 初稿 | SRS 草稿 |
| A5 | 需求验证智能体 | SRS 对 历史需求/对话/项目文档/SRS内部 的交叉验证 | 验证报告 |
| A6 | 需求基线智能体 | 创立基线、生成 RTM、需求差异比对 | 正式SRS、基线、RTM、差异文档 |
| CCB | 变更控制委员会（人工，由本人/教师扮演） | 需求确认审批、变更审批 | 审批决策记录 |

> **⚠️ A1 角色定位（已与教师确认，2026-06-15）**：A1 是**与涉众对话、获取需求的智能体（采访方）**，**不是扮演涉众**。涉众那一侧（课程顾问/教务老师/财务/校长/系统管理员）由**教师在 5000 平台（https://210.34.148.101:5000）提供的 5 个角色 agent** 扮演。`教育培训机构教务收费管理系统需求文档.docx` 就是**与教师那 5 个涉众 agent 对话后收集到的信息**（需求文档开头"你扮演课程顾问"提示词是教师 agent 用的，不是我们的 A1）。
> **交付物（新增）**：要做一个**可实际对话的 Web 页面**，模拟"涉众 ↔ agent"对话场景——**己方=涉众（由教师 5000 平台 5 个 agent 简化代表），对方=我们的 A1 需求获取 agent**。
> **影响**：D02 已建的 5 个 Agent 是按"扮演涉众"建的，**方向反了，需改写为"需求获取/访谈"视角**（见 §10 待办）。

### 2.3 六步工作流（软件设计阶段方法论）
| 步 | 阶段 | 核心产物 | 配套规范 |
|---|---|---|---|
| ① | 架构选型 | ADR-001、ASD | ADR / ASD 规范 |
| ② | 工程产物定义 | MDS + DTS + ADR-002~004 | MDS+DTS 规范 |
| ③ | 约束设计与接口契约 | TLCD 三层约束 + OAS 契约 | TLCD / OAS 规范 |
| ④ | AI 代码生成 | v1 项目源代码 | —（记忆分层注入） |
| ⑤ | 逆向校验 | RCR 逆向校验报告 | RCR 规范 |
| ⑥ | 需求变更→代码变更→归档 | CR→CIA→CRR 闭环 + ADR-005 + 资产包 | CR / CIA / CRR / RTM 规范 |

⚠️ 不是线性瀑布：⑤发现漂移可回退到③改约束或④重生成；⑥变更本质是对整条工作流的再次触发。

### 2.4 工具链
- **Obsidian** 知识库（Karpathy 四层架构，Markdown + 双向链接 `[[]]` + Git 版本控制）。
- **compile.js**：知识库编译验证脚本（结构/命名/链接/基线一致性四类检查；**只验证不修正**）。
- **CrewAI**：多智能体框架（每个 Agent = Role + Goal + Backstory + Tool + Task）。
- **n8n**：工作流自动化引擎（工作流1=需求开发全流程；工作流2=需求变更全流程；含人工审批 Webhook 节点）。
- **PlantUML**：纯文本 UML（用例图/活动图/时序图），可被 AI 读取与版本控制。
- **Reasonix**：AI 代码生成终端（三段式记忆：元记忆/项目记忆/会话记忆；靠分层约束 + 缓存命中率优化）。
- **语言模型（A1~A6 智能体统一）**：**DeepSeek 官方 API**，model `deepseek-v4-pro`，base `https://api.deepseek.com`。API Key 存于 `D:\Project\.env`（已 gitignore，**严禁提交 GitHub**）；CrewAI/LiteLLM 用 `deepseek/deepseek-v4-pro` 前缀、读 `DEEPSEEK_API_KEY`。
- **CodeGraph**：基于 tree-sitter 的静态分析（构建正向图谱=设计意图，逆向图谱=实际代码，自动比对查漂移）。

### 2.5 知识库目录结构（Karpathy 四层 + 不可变约束）
```
项目根/
├── raw/notes/                         # 第1层 原始笔记（涉众原话，写入即不可改）
│   └── {涉众角色}-{YYYYMMDD-HHMM}-需求记录.md
├── wiki/
│   ├── summaries/                     # 第2层 总结提炼（需求清单/UML/SRS初稿/Agent配置，带版本号）
│   │   └── {文档类型}-v{x.y}.md
│   └── baselines/                     # 第3层 基线版本（评审冻结后复制进来，不可改不可删）
│       └── BL-YYYYMMDD-NN/
│           ├── SRS-正式版.md  需求清单.md  UML模型/  溯源矩阵.md
│           ├── adr/ style/ module/ topology/ constraint/ oas/   # 设计产物分目录
│           └── change/ report/                                   # 变更三剑客 + 校验报告
├── archive/                           # 第4层 归档（五层设计资产包）
└── compile.js
```
**两条铁律**：① `raw/notes` 与 `wiki/baselines` 写入后**不得修改、不得删除**；② 需求变更只**新建**基线目录，旧基线完整保留。

### 2.6 编号与命名规范（全套 ID 体系，建议项目简码 = `EDU`，待确认 §8）
| 产物 | 文档编号 | 子项 ID | 备注 |
|---|---|---|---|
| 需求（基线内） | — | `BR/UR/FR/NFR/IFR-模块-NNN`；任务书亦用 `REQ-{模块缩写}-{序号}` | ID 终身唯一、不复用、废弃仅标状态 |
| 基线 | `BL-YYYYMMDD-NN` | — | NN=当日流水号 |
| RTM | `RTM_BL-..._需求溯源矩阵.md` | — | 22 列全字段 |
| ADR | `ADR-NNN` | — | 001~004=初始化，005+=变更 |
| ASD | `ASD-EDU-Vx` | — | 架构风格声明 |
| MDS | `MDS-EDU-Vx` | 模块 `MOD-NNN` | |
| DTS | `DTS-EDU-Vx` | 依赖边 `EDGE-NNNN` | 与 MDS 版本一一对应 |
| TLCD | `TLCD-EDU-Vx` | `C-ARCH-NNNN`/`C-MOD-NNNN`/`C-CODE-NNNN` | 三层约束 |
| OAS | `OAS-EDU-Vx` | 接口 `API-NNNN` | OpenAPI 3.0.3 |
| CR | `CR-EDU-2026-NNNN` | `CR-ITEM-NNN` | 变更需求 |
| CIA | `CIA-EDU-2026-NNNN` | — | 九维影响分析，与 CR 一一绑定 |
| CRR | `CRR-EDU-2026-NNNN` | — | 变更回归校验，与 CR/CIA 绑定 |
| RCR | `RCR-EDU-迭代-日期序号` | 违规 `ERR-NNNN` | 逆向校验报告 |

时间戳统一 `YYYYMMDD-HHMM`（精确到分钟）。YAML 文件 UTF-8 / 2 空格缩进。

### 2.7 三层约束 → 三级校验对应
架构层约束(C-ARCH) ↔ 架构级漂移校验；模块层约束(C-MOD) ↔ 模块边界漂移校验；代码层约束(C-CODE) ↔ 代码规范校验。RCR 六维：架构/模块/拓扑/契约/代码工程/需求溯源。漂移三级：严重(Blocker，必修) / 一般(Major，须改) / 提示(Minor，可评审豁免)。

---

## 3. 工程产物 ↔ 规范文档 索引（写每份产物前先翻对应规范）

| 产物 | 规范文档（`实践规范文档/` 内） |
|---|---|
| SRS | `SRS规范.md` |
| RTM 需求溯源矩阵 | `需求溯源矩阵（RTM）规范.md` |
| ADR（含 ADR-001~005 模板） | `ADR架构决策记录生成规范（AI工程标准版）.md` + `ADR001~ADR005 生成规范 + 标准化模板.md` |
| ASD 架构风格声明 | `架构风格声明生成规范（AI工程标准版）.md` |
| MDS + DTS 模块划分与依赖拓扑 | `模块划分与依赖拓扑生成规范（AI工程统一标准版）.md` |
| TLCD 三层约束 | `三层约束设计生成规范（架构-模块-代码 AI工程标准版）.md` |
| OAS 接口契约 | `OpenAPI YAML 接口契约生成规范.md` + `…使用指南.md` + `…全覆盖范围.md` |
| CR 变更需求 | `变更需求文档（CR）生成规范（AI工程标准版）.md` |
| CIA 变更影响分析 | `变更影响分析报告生成规范（AI工程标准版）.md` |
| CRR 变更回归校验 | `变更回归校验报告生成规范（AI工程标准版）.md` |
| RCR 逆向校验 | `逆向校验报告生成规范（AI工程标准版）.md` |

> 教材原文：`《高级软件设计实践》讲义.docx`（17 节，方法论详解）；任务书：`《高级软件设计实践》实践任务书.docx`（周历/交付物/评分）。

---

## 4. 交付物总清单

### 4.1 期中考核（第 1–2 周 · 需求工程 · 10 项）
1. Obsidian 知识库完整压缩包（Karpathy 四层 + `compile.js`）
2. A1 需求获取智能体配置（每涉众 1 份 CrewAI Agent，Role/Goal/Backstory；**采访方视角**）—— **本项目 5 个** + **可实际对话的 Web 页面**（己方=涉众，对方=A1 agent）
3. 需求获取文档（各涉众需求记录、对话记录）
4. 需求分析产物（需求问题清单 + 回退记录、需求分类清单）
5. UML 建模产物（用例图、核心活动图 PlantUML）
6. SRS 文档（**优秀线 ≥ 15000 字**）
7. 需求验证报告（A5）
8. 需求基线文档（A6 正式 SRS + RTM 溯源矩阵）
9. n8n 工作流 1（需求开发全流程，可一键触发）
10. 缺陷分析报告（**≥ 5 个**）

### 4.2 期末考核（第 3–4 周 · 软件设计 · 9 项）
1. 工程产物包：ASD + MDS + DTS + **ADR ≥ 5 篇**（含 ADR-001 架构选型、ADR-005 变更决策）
2. 约束设计与契约：TLCD 三层约束 + OpenAPI YAML（v1 + 变更后）+ 约束提示词
3. 项目源代码：v1 + v2（含 diff 对比）
4. 逆向校验报告：v1 RCR + v2 CRR（含变更前后图谱对比）
5. ★需求变更管理：CR → CIA → 约束/契约更新 → ADR-005 → n8n 工作流 2 → CRR
6. 子技能文件：**≥ 5 个 SKILL.md**（完备性/正确性/一致性/有效性校验 + 变更影响分析）
7. 设计资产包：五层结构（代码→契约→图谱→子技能→模板）+ 复用指南
8. 综合文档：含规范封面 + **≥ 2000 字批判性反思报告**
9. 演示视频：**≤ 5 分钟**（展示「需求开发→设计→代码」「需求变更→代码变更」）

---

## 5. 一个月二十天详细排程

> 周一至周五为工作日。每天「产出」即当日要落盘到知识库的东西。

### 第 1 周（06-15 ~ 06-19）· 需求工程启动
| 日 | 日期 | 任务 | 产出 |
|---|---|---|---|
| D01 | 06-15 周一 | 课程导入；锁定项目=教育收费系统；搭 Obsidian 四层目录；写 `compile.js` 并跑通；`git init` | 环境验证清单、知识库骨架 |
| D02 | 06-16 周二 | 设计 5 个涉众 CrewAI Agent（Role/Goal/Backstory），配对话界面 | 5 份 Agent 配置 |
| D03 | 06-17 周三 | 需求获取：5 涉众分别与各自 Agent 对话（素材来自需求文档/平台 210.34.148.101:5000）；写入 `raw/notes/` | 涉众需求记录（带时间戳） |
| D04 | 06-18 周四 | 继续获取，补全异常路径/数据边界/痛点追问；覆盖全部模块 | 需求获取记录（全量） |
| D05 | 06-19 周五 | A2 需求分析：检测 模糊/不一致/矛盾/冲突；问题需求回退对应涉众 | 需求问题清单 + 回退记录 |

### 第 2 周（06-22 ~ 06-26）· 建模→SRS→基线（期中冲刺）
| 日 | 日期 | 任务 | 产出 |
|---|---|---|---|
| D06 | 06-22 周一 | A3 建模：用例图 + 核心用例活动图（PlantUML，Guard 条件显式化） | UML 模型 |
| D07 | 06-23 周二 | A4 生成 SRS 初稿（IEEE 830，骨架→填充，精确优先于流畅，冲 ≥15000 字） | SRS 初稿 v0.x |
| D08 | 06-24 周三 | A5 交叉验证（历史/对话/项目文档/SRS内部）；按报告修订 SRS | 验证报告 + SRS 修订 |
| D09 | 06-25 周四 | CCB 确认审批；A6 创立基线 `BL-20260625-01`、生成 RTM；配 n8n 工作流 1 | 正式 SRS、RTM、工作流1 |
| D10 | 06-26 周五 | 缺陷分析 ≥5 个（为 v2 变更埋点）；整理期中提交物；自检 compile 通过 | 缺陷分析报告、期中提交包 |

### 第 3 周（06-29 ~ 07-03）· 架构设计 + 约束 + v1 代码
| 日 | 日期 | 任务 | 产出 |
|---|---|---|---|
| D11 | 06-29 周一 | 知识图谱构建（扫描 SRS→Component/Interface/Constraint 节点）；五维度架构选型 → **ADR-001 + ASD** | ADR-001、ASD、架构选型报告 |
| D12 | 06-30 周二 | 工程产物定义：**MDS（模块划分）+ DTS（依赖拓扑）+ ADR-002~004** | MDS、DTS、ADR-002~004 |
| D13 | 07-01 周三 | **TLCD 三层约束**（C-ARCH/C-MOD/C-CODE）+ **OpenAPI YAML 契约**；构造约束提示词 | TLCD、OAS v1、约束提示词 |
| D14 | 07-02 周四 | Reasonix 分层注入约束，AI 自底向上生成 **v1 代码**（infra→repo→service→controller）；记录 /stats | v1 项目源代码 |
| D15 | 07-03 周五 | CodeGraph 逆向校验：四类漂移检测；四维度质量校验；整改 → **RCR(v1)** | RCR v1、质量校验报告 |

### 第 4 周（07-06 ~ 07-10）· ★需求变更闭环 + 资产归档 + 答辩
| 日 | 日期 | 任务 | 产出 |
|---|---|---|---|
| D16 | 07-06 周一 | 选定 v2 变更场景（从 D10 缺陷中选，如「优惠规则/退费公式/课时扣减」）；写 **CR**；**CIA** 九维影响分析；写 **ADR-005** | CR、CIA、ADR-005 |
| D17 | 07-07 周二 | 更新约束/契约（OAS 版本递增、DTS/TLCD 同步）；AI 驱动 **v1→v2 增量变更** | OAS v2、v2 代码、diff |
| D18 | 07-08 周三 | 回归校验：v2 逆向比对 + 漂移再检测 → **CRR**；CCB 变更审核；新基线 `BL-2026xxxx-02` + RTM 更新；n8n 工作流 2 | CRR、新基线、工作流2 |
| D19 | 07-09 周四 | 开发并封装 **子技能 ≥5 个**（完备/正确/一致/有效校验 + 变更影响分析）；整理 **五层设计资产包** + 复用指南 | SKILL.md ×5、资产包 |
| D20 | 07-10 周五 | 期末综合文档（含 ≥2000 字批判性反思）；5 分钟演示视频；答辩与验收 | 综合文档、演示视频、PPT |

> 缓冲提示：D10 与 D15 是两个硬节点（期中提交、v1 校验通过）。若进度滞后，优先保 SRS 质量（期中 40%）与变更闭环完整性（期末「★需求变更」是评分核心，缺 ADR-005 或回归校验不完整会直接掉档）。

---

## 6. 评分标准与达标线

- **总成绩** = 平时 20% + 期中 40% + 期末 40%。课程目标达成度 ≥ 0.60 视为达成。
- **期中优秀线**：SRS ≥15000 字；知识库 compile 正常；涉众 Agent ≥4 个（本项目 5 个）；n8n 工作流1 一键触发；缺陷分析 ≥5 个；基线创立规范。
- **期末优秀线**：工程产物完整高质量；代码通过校验（**漂移 ≤2 处**）；★需求变更闭环完整（CR→影响分析→契约更新→代码变更→回归校验→ADR-005→新基线）；子技能规范可复用；反思深刻。
- **红线（直接掉档/不合格）**：知识库无法编译；工作流1 无法触发；缺失需求变更管理环节；代码无法编译；基线被私自修改。

---

## 7. 系统功能范围（5 涉众模块清单 · 建模与 SRS 边界）

> 这是 SRS 功能需求与后续 MDS 模块划分的原始素材。详细字段/业务规则见需求文档 docx 对应段落。

- **课程顾问**：① 客户咨询与线索登记　② 课程推荐与试听安排　③ 报名流程与订单生成　④ 价格优惠与缴费引导　⑤ 客户跟进与转化统计　⑥ 权限与使用体验（移动端，查名额/收款响应 ≤3s）
- **教务老师**：① 排课管理（师/室/班/课/生/时段冲突检测）② 班级管理（一对一/小班/大班、容量、候补）③ 考勤与消课（扫码/点名/家长小程序，按课程扣课时规则）④ 请假调课补课（审批、补课有效期 30 天）⑤ 转班与课程变更（差价计算、赠送课时处理）⑥ 教师课时统计（按实际考勤）
- **财务人员**：① 收款与订单财务记录　② 预收款与收入确认（逐节确认、按总课时含赠送分摊）③ 退费流程（多级审批、退费单）④ 欠费与续费提醒　⑤ 发票管理（含红冲/作废）⑥ 支付对账（微信/支付宝/银行/现金，待认领、重复支付）⑦ 教师工资与成本核算（赠课计成本）⑧ 财务报表
- **校长**：① 经营数据看板（营收/续费/招生/退费/课消/满班率）② 招生与转化分析（渠道 ROI、顾问排行）③ 教学运营分析　④ 财务与资金安全监控（异常报警）⑤ 多校区管理（数据隔离 + 汇总 + 横向对比）⑥ 审批管理（退费/优惠/改价/课时/欠费上课）⑦ 移动端与家长小程序
- **系统管理员**：① 账号与组织架构（总部→校区→部门→岗位）② 权限模型（角色×校区双维、操作级、数据范围、临时授权）③ 业务参数配置（课时/请假/退费/优惠/审批流/通知模板/支付参数）④ 部署性能扩展（阿里云、~500 用户/100 并发、报名缴费秒级）⑤ 数据备份与灾难恢复（全量+增量、异地、RPO≈5min、RTO≈4h）⑥ 支付接口与第三方对接（微信/支付宝/短信/电子发票/小程序）⑦ 日志审计（高风险操作、保留≥3年、不可删）⑧ 安全（强密码/脱敏/防越权/防重复提交/验签/软删除）

**MDS 候选业务模块（D12 用）种子**：客户线索 / 课程班级 / 排课 / 报名订单 / 课时账户·考勤 / 收费支付 / 退费 / 收入确认 / 发票 / 对账 / 教师课时·工资 / 经营分析BI / 校区组织 / 账号权限RBAC / 系统配置 / 通知 / 审批流 / 日志审计；**通用基础模块**：支付网关 / 第三方对接 / 短信 / 缓存 / 定时任务。

---

## 8. 决策记录（已确认 ✅ / 默认沿用 ◻️）

1. ✅ **技术栈** = **Java + Spring**，Controller→Service→Repository→Model 四层分层架构（对齐讲义与 ADR 模板，ADR-001 据此撰写）。
2. ✅ **智能体语言模型**（A1~A6 统一）= **DeepSeek 官方 API**，model `deepseek-v4-pro`，base `https://api.deepseek.com`。**API Key 存 `.env`（已 gitignore，严禁提交）**，主控文档与任何受版本管理的文件中只记位置、不记 Key 明文。
3. ✅ **我的角色** = **全量执行**：我直接产出全部文档（SRS/ADR/ASD/MDS/DTS/TLCD/OAS/RTM/CR/CIA/CRR/RCR）与 v1/v2 代码、SKILL、脚本，你逐项审阅拍板。
4. ✅ **工具环境** = **本机等效方案落地**（不依赖课程平台专有工具），映射如下：
   | 课程工具 | 本机等效落地 |
   |---|---|
   | Obsidian 知识库 | 本地 Markdown 四层目录 + 双向链接 + Git |
   | compile.js | 我写 Node.js 校验脚本（结构/命名/链接/基线一致性） |
   | CrewAI 涉众智能体 | 我写 5 份 Python Agent 定义文件（Role/Goal/Backstory），作为交付物 |
   | Reasonix 代码生成 | **由我（Claude Code）按三层约束 + OAS 契约分层生成 v1/v2 代码** |
   | CodeGraph 逆向校验 | 我写脚本从生成代码提取实际依赖/接口图谱，与 MDS+DTS+OAS 正向图谱比对，产出 RCR |
   | n8n 工作流 | 我产出工作流 JSON 导出件 + 节点链路说明（需求开发流程1 / 变更流程2） |
5. ✅ **工作目录** = `D:\Project` 根目录（知识库直接建在此，不另开子文件夹）。**远程仓库** = https://github.com/saucecat114514/Software-design-practice （D01 已完成本地 init + 连 origin + 首提 + push，main 跟踪 origin/main）。
6. ◻️ **项目简码** = `EDU`（编号形如 ASD-EDU-V1；如需更换告知我）。
7. ◻️ **进度起点** = 今天 D01 全新开始（首次对话）。
8. ◻️ **涉众需求来源** = 默认以根目录需求文档 docx 作为涉众原话素材驱动 A1 对话记录；若你已在平台 `210.34.148.101:5000` 跑过对话，把记录贴给我即可替换。

---

## 9. 进度看板（完成即打勾，附日期）

**期中（需求工程）**
- [x] 知识库四层 + compile.js 跑通（2026-06-15，0 错误 0 警告；已 push 到 GitHub）
- [x] 5 个 A1 需求获取 Agent（2026-06-15，已按"采访方"视角改写；采访行为冒烟测试通过）
- [x] A1 对话 Web 页面（2026-06-15，`agents/web/`，本地后端+前端，密钥服务端保管；端到端测试通过）
- [x] 需求获取记录（raw/notes 全量）—— 2026-06-16 五类涉众全部完成 A1 访谈并落盘（课程顾问/教务/财务/校长/系统管理员），约 133 条 REQ
- [x] 需求问题清单 + 回退记录（2026-06-16，A2 检出 24 条 ISSUE：模糊/不一致/矛盾/冲突；附回退记录；合并版需求清单 125 条 REQ）
- [x] UML 用例图 + 活动图（2026-06-21，A3：6 用例图 + 6 核心活动图，PlantUML 全部 -checkonly 通过并渲染 PNG；`wiki/summaries/uml/`，说明见 `UML建模说明-v1.0.md`）
- [x] SRS 初稿（≥15000 字）（2026-06-21，A4：`wiki/summaries/SRS-初稿-v0.1.md`，约 9.9 万字，IEEE830/GB9385 全结构，86 条 FR + 5 IFR + 7 类 NFR + E-R/架构图/变更流程；compile 0 错 0 警）
- [x] A5 验证报告（2026-06-21，`wiki/summaries/需求验证报告-v1.0.md`，程序确定性检查 + 5 子系统语义验证 + CCB 复核处置；据此修订 SRS→v0.2）
- [x] 基线 BL-20260623-01 + RTM（2026-06-23，A6 + CCB 批准：`wiki/baselines/BL-20260623-01/`，SRS正式版V1.0.0 + 22列RTM(110行) + CCB评审 + 需求清单 + 12 UML；compile 基线检查通过）
- [x] n8n 工作流 1（2026-06-23，`automation/n8n_工作流1_需求开发全流程.json`，A1→A6 + CCB 人工审批 Webhook + 3 回退环，15 节点可导入一键触发）
- [x] 缺陷分析 ≥5（2026-06-23，`wiki/summaries/缺陷分析报告-v1.0.md`，7 条缺陷，6 条作 v2 CR 埋点）

**期末（软件设计）**
- [x] ADR-001 + ASD（2026-06-26，D11：知识图谱 KG-EDU-V1[78节点/228边] + 架构选型评估报告[四层4.82>DDD2.72>微服务2.44] + ADR-001[已接受] + ASD-EDU-V1[正式生效]，CCB 通过；`wiki/summaries/design/`；compile 0 错 0 警）
- [x] MDS + DTS + ADR-002~004（2026-06-26，D12：build_mds_dts.py 据 KG 派生 21 业务+8 基础=29 模块 + 34 EDGE；FR 88/88 全覆盖无重叠、37/37 组件唯一归属、业务依赖环 0；ADR-002 适中粒度[已接受]/ADR-003 强收敛边界[已接受]/ADR-004 白名单拓扑[已接受]；MDS-EDU-V1 + DTS-EDU-V1 正式生效；CCB 通过；compile 0 错 0 警）
- [x] TLCD + OAS v1 + 约束提示词（2026-06-26，D13：TLCD-EDU-V1 三层约束 38 条[C-ARCH12/C-MOD12/C-CODE14]逐条溯源 + OAS-EDU-V1[OpenAPI3.0.3，台账93接口全登记/核心12接口完整YAML] + 约束提示词[固定前缀+变动后缀]；CCB 通过、均正式生效；compile 0 错 0 警、YAML 合法）
- [x] v1 代码（2026-06-26，D14：`code/edu-charge-system` Spring Boot 3.2.5 四层分层单体，**41 Java 文件/1390 LOC，Maven BUILD SUCCESS**；完整 infra + 退费 MOD-010★/转班 MOD-007★ 完整业务逻辑[DEF-001/004 口径]+收入确认事件解耦+报名算价+基础模块桩；约束自查零违规[Controller直连Repo=0/裸SQL=0/事务仅Service]；其余模块台账登记随补）
- [x] RCR v1（漂移 ≤2）（2026-06-26，D15：codegraph_rcr.py 逆向解析 41 类六维比对[架构/模块/拓扑/契约/代码/溯源]；初检 1 Minor[OrderServiceImpl 阈值硬编码]→整改入配置注入→复测 **0 违规/Blocker=0/关键漂移 0**，远超漂移≤2；RCR-EDU-v1-20260626-01 整改完成、允许交付；mvn 复编译 BUILD SUCCESS）
- [x] CR + CIA + ADR-005（2026-06-26，v2 变更立项：CR-EDU-2026-0001[P0,5 ITEM:资源IFR/NFR降级+退费/转班口径+持久化可运行] + CIA-EDU-2026-0001[九维,变更可控] + ADR-005[基线迭代优化,已接受]；CCB 通过、CR/CIA 正式生效；变更前 git tag `v1-baseline`@f646a0f）
- [ ] OAS v2 + v2 代码 + diff
- [ ] CRR + 新基线 -02 + 工作流 2
- [ ] 子技能 ≥5
- [ ] 五层资产包 + 复用指南
- [ ] 综合文档（≥2000 字反思）+ 演示视频

---

## 10. 进度日志 / 决策记录（持续追加）

- 2026-06-15：通读全部材料（讲义17节 + 任务书 + 需求文档 + 14份规范），建立本主控文档。已确认三项决策：技术栈 Java+Spring、我全量执行、本机等效方案落地（§8）。
- 2026-06-15 · 确定智能体 LLM = DeepSeek 官方 API（model `deepseek-v4-pro`）。API Key 已存入 `.env` 并经 `.gitignore` 排除 + 实地验证（`!! .env` 被忽略、git grep 全库搜不到 Key），**绝不上传 GitHub**；新增 `.env.example` 模板。
- 2026-06-15 · **D01 完成**：搭好 Karpathy 四层知识库（raw/notes、wiki/summaries、wiki/baselines、archive）+ 各层说明；写 compile.js 并跑通（0 错误 0 警告）；.gitignore（云盘式）+ README；git init→首提（root-commit f9d2bfa，25 文件，含源材料 docx 与 14 份规范）→push 到 https://github.com/saucecat114514/Software-design-practice （main 跟踪 origin/main）。git 本地身份 = saucecat114514 / ckyatnz@gmail.com。**下一步 D02**：设计 5 个涉众 CrewAI Agent（Role/Goal/Backstory）。
- 2026-06-15 · 连通性确认：DeepSeek `deepseek-v4-pro` 接口可用，密钥从 `.env` 加载正常（`wiki/summaries/agents/test_connection.py`）。
- 2026-06-15 · **D02 完成**：5 个涉众 CrewAI Agent（课程顾问/教务老师/财务/校长/系统管理员）配置落在 `wiki/summaries/agents/`，Role/Goal/Backstory 取材自需求文档真实内容；统一 LLM 配置 `llm_config.py` + 共享提示词 `prompt_kit.py`（身份隔离+对话策略）；设计文档 `wiki/summaries/涉众智能体设计-v1.0.md`。课程顾问在场冒烟测试通过（完全在角色内作答）。compile 通过（22 md/0 错）。`.gitignore` 补 `__pycache__`。**下一步 D03**：逐涉众驱动对话获取需求，写入 `raw/notes/`。
- 2026-06-15 · **重要纠正（已与教师确认）**：A1 的定位之前理解反了。A1 = **与涉众对话获取需求的采访方**，不是扮演涉众；涉众侧由**教师 5000 平台的 5 个角色 agent** 扮演；需求文档.docx 是与教师那 5 个 agent 对话后收集的产物。**新增交付物**：可实际对话的 Web 页面（己方=涉众，对方=A1 agent）。**待办**：① 把 D02 的 5 个 Agent 从"扮演涉众"改写为"需求获取/访谈"视角（Role/Goal/Backstory + 设计文档）；② 设计并实现对话 Web 页面。
- 2026-06-15 · **D02 返工完成**：① 5 个 A1 Agent 全部改写为"采访方"视角（`prompt_kit.py` 改为 INTERVIEWER_FRAME+访谈技巧+记录规范；各 agent Backstory 内置该涉众追问清单）；② 新建对话 Web 页面 `agents/web/`（`server.py` 标准库后端+`index.html` 前端，己方=涉众/对方=A1 agent，密钥服务端保管不下发）；③ 设计文档升级 `涉众智能体设计-v2.0.md`（删 v1.0，git 留史）。测试：连通性 ✔、课程顾问采访行为 ✔、后端 `/api/agents`+`/api/chat` ✔。compile 通过。
- 2026-06-15 · **D03 起步 + 对话页面 bug 修复**：用对话页面访谈了课程顾问（覆盖客户信息查询/线索、试听名额锁定改期候补、报名算价审批转介绍），整理成 `raw/notes/课程顾问-20260615-1531-需求记录.md`（含需求清单 REQ-CRM/TRIAL/ORDER + 完整对话原文）。修复页面两个 bug：①`server.py` 保存路径 `parents[3]`→`parents[4]`（之前误写到 `D:\Project\wiki\raw`）；②"整理需求"输出被 `max_tokens=900` 截断 → 前端传 4096 + 强化"通览全程不漏模块"提示词。**遗留**：`D:\Project\wiki\raw` 错位目录待用户确认删除（自动审核拦了强删，内含一份与新记录重复的旧存档）。**下一步**：续访教务/财务/校长/系统管理员。
- 2026-06-15 · **对话页面新增"恢复"能力**（应对关服务丢对话）：① 浏览器 localStorage 每轮自动存，刷新/重开页面选回该涉众即自动恢复；② 新增「恢复上次保存」按钮 + 后端 `/api/last`（保存时同步写 `web/sessions/{agent}.json` 恢复点），恢复不到则提示"无法恢复"。`sessions/` 已 gitignore。已起服务回归测试四种情形全过。
- 2026-06-15 · **数据清理（用户确认）**：① 删除错位目录 `D:\Project\wiki\raw`（bug 产物）；② 1547（页面存档，25 条）比 1531（21 条）多出的 4 条续访（报名环节优惠冲突/审批/转介绍的最终结论）已并入 1531——追加原文 + 升级 REQ-ORDER-002（待澄清→明确"按最高优惠+审批"）/003（声音震动强提醒+倒计时）/004（手机号秒匹配+规则摘要），随后删除 1547。raw/notes 现仅留合并版 `课程顾问-20260615-1531-需求记录.md`。
- 2026-06-15 · **彻底修复 A1 输出截断**：之前只给"整理需求"加了 token 预算，但**普通访谈轮**内联输出多条 REQ 时仍被默认 900 截断（教务老师访谈第 7 条就断在"…毕竟"）。改为根因修复——`llm_config.chat_complete()` 检测 `finish_reason=="length"` 后自动追加"接着上文继续"指令续写、拼接成完整文本（每轮 2048、最多 6 轮）；`server.py /api/chat` 改用 `chat_complete`。测试：每轮压到 256 仍输出完整 12 条、结尾正常。
- 2026-06-16 · **D03 需求获取完成 + D05(A2 需求分析)完成**：5 类涉众访谈全部落 `raw/notes/`（约 133 条 REQ）。建 A2 智能体 `agents/agent_analyst.py` + 运行器 `agents/run_analysis.py`：先逐角色蒸馏去重→`wiki/summaries/需求清单-v1.0.md`（125 条 REQ），再四维质量检测→`wiki/summaries/需求问题清单-v1.0.md`（24 条 ISSUE：模糊6/不一致11/矛盾5/冲突≈5；含获取类/分析类回退记录）。A2 自报统计小计略有偏差，以 24 条清单为准。**下一步**：CCB 评审问题清单/裁决冲突 → 回退补访（如需）→ A3 建模(UML) → A4 SRS 初稿。
- 2026-06-16 · 新增 `wiki/summaries/补访问题清单-v1.0.md`（17 题，按涉众分组，可直接访谈；标注冲突类待CCB）。
- 2026-06-21 · **对话页面支持补访**：① 顶部「补访模式」开关 + 待澄清提纲框 + 「载入待澄清问题」按钮（后端 `/api/followup` 从补访问题清单按涉众取题）；② 补访模式下 A1 只围绕提纲逐条追问、不重头问（`/api/chat` 注入 agenda）；③ 补访保存为单独的 `{涉众}-{ts}-补访记录.md`、会话与首访分键互不覆盖（`/api/save` kind、`/api/last` kind）。端到端测试通过。
- 2026-06-21 · **5 类涉众补访完成 + A2 补访整合**：5 份补访记录落 `raw/notes/`（共 ~102KB）。建 `agents/run_followup_integration.py`（A2 `integrate_prompt`），逐涉众把补访澄清对回问题清单 → `wiki/summaries/需求问题清单-v2.0.md`：模糊/不一致/矛盾类多数 `已澄清`（带具体数字与需求修订建议），跨涉众冲突 ISSUE-007/008/015/023 标 `待CCB裁决`（附各方立场）。**回退环已收口**。
- 2026-06-21 · **CCB 裁决完成**：`wiki/summaries/CCB裁决记录-v1.0.md`，24 条 ISSUE 全部收口——5 条冲突已裁(007退费用财务总课时均价+家长端分项展示/008采纳校长三级审批/015赠课成本入销售费用/022试听锁定按含缓冲占用/023线下拆分仅财务+留痕)、3 条部分澄清补全(017实时分级/018报警集中/024统一告警中心)、5 条分析类统一(004通知渠道/009候补分场景/010考勤状态机/014对账增量/016审批快照)、11 条采纳补访澄清。**无遗留待决**。
- 2026-06-21 · **D06（A3 建模）完成**：建 A3 建模智能体 `agents/agent_modeler.py` + 运行器 `agents/run_modeling.py`（按 5 子系统切片需求清单、注入 CCB 裁决，调 DeepSeek 生成 PlantUML）。产出 `wiki/summaries/uml/`：**6 张用例图**（00 总图 + uc_1~5 五子系统）+ **6 张核心活动图**（act_01 报名优惠审批 / 02 试听锁定候补 / 03 排课冲突 / 04 考勤课时 / 05 退费审批 / 06 日终对账），活动图把 CCB 裁决阈值/公式/时限落进显式 Guard。全部经 PlantUML 1.2024.7（`D:\tools\plantuml.jar`，本机 JDK21+Graphviz）`-checkonly` 校验 + 渲染 PNG（`uml/render/`）。说明与「用例→REQ」「活动图→CCB裁决」追溯表见 `wiki/summaries/UML建模说明-v1.0.md`。compile 通过(39 md/0 错0 警)。**踩坑**：截断重画(extract取末图)/推理吃空(max_tokens→8000)/行尾'注释非法(挪行首)/全角；当结束符(强制ASCII;)/note across在stop后报错(改legend)/泛化方向错(人工校正)/泳道声明不切换(改prompt重生成)。
- 2026-06-21 · **D07（A4 SRS 初稿）完成**：建 A4 需求文档智能体 `agents/agent_srs.py` + 运行器 `agents/run_srs.py`（按《SRS规范》GB9385/IEEE830 模板分章生成：§1引言+§2总体描述 / §3.1 FR×5子系统 / §3.2 IFR+§3.3 NFR / §3.4 数据需求；按子系统切片需求+注入CCB裁决+用例锚点；分章落 `srs_parts/`（gitignore）再拼装）。产出 **`wiki/summaries/SRS-初稿-v0.1.md`，约 9.9 万字（远超 15000 优秀线）**：含修订史、§1~§5 全结构、**86 条 FR**（含优先级/角色/前置/触发/流程/业务规则/输入输出/后置/异常/验收/追溯，CCB裁决阈值公式悉数落进业务规则）、**5 条 IFR**、**7 类 NFR**（指标全量化）、**§3.4 数据需求**（E-R Mermaid + 数据字典 + 数据管理策略）、§2.1 四层架构图 Mermaid、§4 基线与变更管理（Mermaid 流程）、§5 附录。compile 通过(0 错0 警)。**踩坑**：①数据章 E-R 命中 chat_complete 截断重画致 mermaid 画两遍/围栏奇数→删残片保完整；②架构图 mermaid 子程序节点（双方括号形）被 compile 的双链正则误判为断链→改圆角矩形形状。
- 2026-06-21 · **D08（A5 需求验证）完成**：建 A5 验证智能体 `agents/agent_verifier.py` + 运行器 `agents/run_verification.py`（程序确定性检查：REQ覆盖率/模糊词/FR编号与交叉引用；+ 逐子系统 LLM 语义验证：覆盖保真/裁决一致/越界/内部质量；+ revise 模式据修订清单改写）。产出 **`wiki/summaries/需求验证报告-v1.0.md`**：5 子系统共 60 条候选问题，附**第三节 CCB 复核处置**（采纳并修订约38/备案保留约14/驳回4）。据此把 SRS 修订为 **V0.2.0**（同 `SRS-初稿-v0.1.md`，约10.1万字）：消 FR 重号4（FR-RPT/REF/FIN/PAY 改名 TRP/FRP/TREF/FMN/PAYIF）、修断引用3（INV-003/004→INV-001、删 SCH-006）、补覆盖（CRM-004/005 内容+追溯、新增 FR-BIZ-003/FR-DR-003、ACC 界面标识）、对齐 CCB（候补排序付费>紧急度>时间、试听性能P99≤3s/目标≤1s/>5s告警、迟到到课即扣、临时病假凭证免扣、退费三级绝不自动通过+值班校长、审批参数快照、单边待认领账术语）、收敛越界字段、量化模糊词。**驳回 A5 误判4**（退费公式赠课不退现/普通请假>2h不扣/考勤当日=自然日/转班单价口径）。内联复验：FR 88条无重号、交叉引用全可解析、REQ全覆盖；compile 0错0警。**踩坑**：run_verification 的 prog 模式会覆盖报告→改用内联脚本复验避免清掉手写处置结论；主控日志里字面写双方括号会被 compile 双链正则误判→改述。
- 2026-06-23 · **D09（CCB 确认 + A6 创立初始基线）完成**：建 A6 基线智能体 `agents/agent_baseline.py`（BR 业务目标层 8 条 + 模块→BR/子系统/建模 映射 + CCB 评审记录）+ 运行器 `agents/run_baseline.py`（**程序化确定性**生成 22 列 RTM + 冻结基线包）。**CCB 评审通过**：确认 A5 处置（采纳38/驳回4）、终裁 8 项备案（缓冲5min可配[1~30]、折价系数默认1.0[0~1]、发票阈值5000、续费提醒7天/3天/3次、请假率周20%/月30%、灾备RTO30min、转介绍来源保留、撞单姓名脱敏）。**创立初始基线 `wiki/baselines/BL-20260623-01/`**：`SRS-正式版-v1.0.md`(V1.0.0定版,264KB) + `RTM_BL-20260623-01_需求溯源矩阵.md`(110行=93FR/IFR+17NFR，BR→UR(REQ)→SRS-ID→建模ID→设计/开发/测试占位，状态全"新增") + `CCB_..._评审记录.md` + `需求清单-v1.0-基线.md` + `UML模型/`(12 puml) + `_基线说明.md`。初始基线无历史版本→依 RTM 规范 Q4 不生成 DIFF。compile 第5节基线检查通过(0错0警，已创立基线1个)。
- 2026-06-23 · **D10（n8n 工作流1 + 缺陷分析 + 期中收口）完成**：① `automation/n8n_工作流1_需求开发全流程.json`（15 节点可导入 n8n：手动触发→A1→A2→IF冲突回退补访→A3→A4→A5→IF不过回退修订→**CCB 人工审批 Wait/Webhook**→IF退回回 A2→A6→归档；executeCommand 跑本机 agent 脚本；3 处回退环 + 1 人工审批；JSON 经校验节点/连接无悬空）+ `automation/README.md`。② `wiki/summaries/缺陷分析报告-v1.0.md`：**7 条缺陷**（DEF-001 退费二级中间档为推断/002 缺时序图/003 NFR无条目ID/004 转班vs退费单价口径未对齐/005 通知降级SLA缺失/006 备案默认值待复核/007 A4长结构无防截断），6 条作期末 CR 埋点。③ `期中提交说明.md`：优秀线 6 项全达成对照 + 交付物索引 + 自检。compile 0错0警。**第一阶段（需求工程·期中40%）全部完成**。**下一步（第3-4周·软件设计·期末40%）**：知识图谱→ADR-001+ASD→MDS/DTS→TLCD/OAS v1+约束→v1代码→RCR(漂移≤2)→**★需求变更闭环**(择 DEF-001/004 走 CR→CIA→契约/代码→CRR→ADR-005→新基线 BL-…-02)→OAS v2+v2代码+diff→工作流2→五层资产包+反思。
- 2026-06-26 · **D11（知识图谱 + ADR-001 + ASD）完成 · 期末软件设计阶段启动**。归档约定：设计产物先放 `wiki/summaries/design/` 工作层迭代，期末成套后再整体冻进新基线目录，需求基线 BL-20260623-01 保持不可变。产物三件：
  ① **知识图谱 KG-EDU-V1**（`design/knowledge-graph.json` + `design/知识图谱-v1.0.md`）：新建 `agents/build_knowledge_graph.py` **确定性解析**基线 RTM（无 LLM、可复现），抽出 **78 节点**（Component 37 / Interface 5 / Constraint 25[17 NFR+8 CCB 量化] / BaseModule 10 + 系统根）+ **228 边**（realizes/traces_to/depends_on/constrained_by/served_by_base）；含子系统×组件×FR 分布、Mermaid 子系统拓扑、五维度量化输入。作为 ADR-001 选型底座 + D12 MDS/DTS 种子 + D15 RCR 正向基准。
  ② **架构选型评估报告**（`design/架构选型评估报告-v1.0.md`）：教材五维度（功能复杂度/并发性能/可扩展性/团队规模/运维能力）+2 工程维（AI 代码生成适配性/落地成本）加权打分——**四层分层 4.82 ≫ DDD 2.72 / 轻量微服务 2.44**。
  ③ **ADR-001**（`design/adr/ADR-001_系统顶层架构风格选型决策.md`，10 字段 Markdown + 末尾 YAML 约束块）+ **ASD-EDU-V1**（`design/style/ASD_V1_…分层架构风格声明.md`，9 字段，强制/允许/禁止三类规则 + AI 生成专属约束 + 9 项漂移校验量化标准[Blocker=0/总漂移≤2]）。**最终决策=Controller→Service→Repository→Model 四层分层、单体、强制单向**；辅助风格=无（缓存/消息/定时/审批为基础模块辅助手段）。CCB 评审通过（`design/CCB_ADR001-ASD_评审记录-v1.0.md`）：ADR-001 提议→**已接受**、ASD 草稿→**正式生效**。compile 0 错 0 警（63 md）。**下一步 D12**：ADR-002 模块粒度 + ADR-003 职责数据边界 + ADR-004 依赖通信拓扑 → MDS + DTS（以 KG-EDU-V1 的 37 组件/6 子系统/10 基础模块为种子）。
- 2026-06-26 · **D12（MDS + DTS + ADR-002~004）完成**。新建 `agents/build_mds_dts.py` 以 KG-EDU-V1 为唯一源**确定性派生**中层架构成对产物，并程序化校验通过：
  ① **MDS-EDU-V1**（`design/module/MDS_V1_…模块划分方案.md` + `design/module-catalog.json`）：把 37 组件按**适中业务域粒度**归并为 **21 业务模块 + 8 通用基础模块 = 29 模块**（MOD-001~021 业务 / MOD-101~108 基础），各模块归一 Spring 服务，含职责/Include/Exclude/一表一归属/FR 溯源。**校验：FR 88/88 全覆盖、重叠 0、未覆盖 0；37/37 组件唯一归属、重复 0、缺失 0**。跨域归并：PAYIF→财务 MOD-009、ALERT→bi MOD-018（按业务能力非提出方）。
  ② **DTS-EDU-V1**（`design/topology/DTS_V1_…依赖拓扑方案.md` + `design/dependency-topology.json`）：**34 条有向白名单 EDGE**（业务→基础 19 / 跨域 Service→Service 10 / 业务→外部 IFR 5）+ 全局基础矩阵规则（RBAC/缓存/审计）+ 层级依赖矩阵 + 禁止黑名单；弱一致跨域（课消→收入确认、收款→开票、退费→红冲）用**领域事件**解耦。**校验：业务依赖环 = 0（DAG）**。
  ③ **ADR-002**（适中粒度，粗/适中/细三方案）+ **ADR-003**（强收敛边界，松散/强收敛二选一，一表一归属）+ **ADR-004**（白名单受控依赖，自由/白名单二选一，本地同步/事件异步分工 + 黑名单）——均 10 字段 + ≥2 方案比对，CCB 评审通过（`design/CCB_ADR002-004-MDS-DTS_评审记录-v1.0.md`）：三 ADR **已接受**、MDS/DTS **正式生效**。compile 0 错 0 警。**下一步 D13**：TLCD 三层约束（C-ARCH/C-MOD/C-CODE）+ OpenAPI YAML 契约 OAS v1 + 约束提示词（继承 MDS 边界 + DTS 白名单）。
- 2026-06-26 · **D13（TLCD + OAS v1 + 约束提示词）完成**。三件套：
  ① **TLCD-EDU-V1**（`design/constraint/TLCD_V1_…三层约束设计规范.md`）：手写三层约束 **38 条**——C-ARCH 12（溯源 ASD/ADR-001：四层单向/禁穿透反向/通信范式/技术栈红线/配置注入/NFR 全局）+ C-MOD 12（溯源 MDS/DTS/ADR-002~004：一表一归属/白名单 34 边/无环/基础单向/事件解耦/财务口径独占）+ C-CODE 14（溯源 ASD§4：目录即层级/命名/异常/日志/事务仅 Service/禁魔数/统一响应）。每条含溯源+漂移判定+等级（Blocker/Major/Minor），合规线 Blocker=0/总漂移≤2。
  ② **OAS-EDU-V1**：新建 `agents/build_oas_inventory.py` 从 module-catalog+RTM **确定性生成全量接口台账** `design/oas/接口清单-API-Inventory-v1.0.md`（**93 接口=88FR+5IFR 全登记 API-NNNN**，绑定 MOD/RTM/服务/方法，满足"零遗漏"台账层）；手写核心业务主线**完整 OpenAPI 3.0.3 YAML** `design/oas/OAS_V1_…接口契约.yaml`（**13 操作**：报名算价/下单·收款认领/对账·签到课时·收入确认·**退费计算/审批**·**转班费用**·开票·支付回调·通知 + 登录；含 components 23 schema/security/parameters/responses/x-extension 溯源），yaml 解析通过、3.0.3 规范正确。**退费(FR-REF-001/003)/转班(FR-TRF-002) 为 v2 变更目标，已写完整契约含 DEF-001/004 口径**。如实声明：其余模块台账登记、YAML 体随开发补全（与 v1 代码代表性切片一致）。
  ③ **约束提示词**（`design/constraint/约束提示词-AI代码生成-v1.0.md`）：固定前缀（C-ARCH+C-CODE 全局，逐字复用命中缓存）+ 变动后缀（C-MOD 边界+OAS 契约，按模块替换），含 MOD-010 退费完整实例，供 D14 直接注入。
  CCB 评审通过（`design/CCB_TLCD-OAS_评审记录-v1.0.md`）：TLCD/OAS/提示词均 **正式生效**。compile 0 错 0 警、YAML 合法。**下一步 D14**：AI 分层注入约束提示词，自底向上生成 **v1 代码**（infra→repo→service→controller），记录 /stats。
- 2026-06-26 · **D14（v1 代码）完成**。`code/edu-charge-system`（Spring Boot 3.2.5 + Java 17，四层分层单体），AI 分层注入约束提示词自底向上生成，**Maven `clean compile` BUILD SUCCESS（41 源文件/1390 LOC/release 17）**。范围=以 v2 变更目标为核心的代表性纵切：
  ① **infra/common**：统一响应 ApiResponse{code,message,data,timestamp}、BizException+ErrorCode「{域}-{编号}」+全局异常处理器、ChargeProperties（CCB 阈值配置注入，禁魔数）、通知/支付网关基础模块（接口+桩，手机号脱敏）。
  ② **MOD-010 退费 ★**（完整四层 12 文件）：退费计算口径 DEF-001（含赠送总课时均价+先购买后赠送消耗顺序+折价系数配置注入+参数快照冻结）+三级分级审批（大额绝不自动通过）+ 已开票阻断 + 退费通过领域事件。
  ③ **MOD-007 转班 ★**（完整四层 9 文件）：差价按实际支付单价口径 DEF-004 + 赠课作废/结转 + 原路退款经 EDGE-0023 跨域调 RefundService（Service→Service 白名单）。
  ④ **MOD-012 收入确认** + **RefundEventListener**：订阅退费事件做红冲（EDGE-0026 弱一致事件解耦，证明 DTS 拓扑落地）。
  ⑤ **MOD-003 报名算价**：互斥优惠校验 + 占比超阈触发审批。
  约束自查零违规（Controller 直连 Repository=0/Service 裸 SQL=0/非 Service 层 @Transactional=0/阈值全配置注入）；统计与自查见 `code/edu-charge-system/STATS.md`。Java 构建产物 target/ 已 gitignore。**下一步 D15**：CodeGraph 逆向校验四类漂移 + 四维度质量校验 → RCR(v1)，目标漂移 ≤2。
- 2026-06-26 · **D15（CodeGraph 逆向校验 + RCR v1）完成**。新建 `agents/codegraph_rcr.py`（本机等效 CodeGraph：正则静态解析 v1 的 41 个类→实际分层/模块/依赖逆向图谱，与正向基线 module-catalog/dependency-topology/OAS/TLCD **六维比对**：架构/模块/拓扑/契约/代码/溯源，违规分级 Blocker/Major/Minor）。流程：
  ① **初检**：先误报 4 Blocker（契约维路径正则把类级 @RequestMapping 当方法级致基路径重复拼接）→ 修正正则（仅匹配方法级 Mapping）后真实结果 = **1 Minor**（ERR-0001：OrderServiceImpl 审批占比阈值 0.25 硬编码，违反 C-ARCH-0008 配置注入）。
  ② **整改闭环**：阈值迁移至 `ChargeProperties.orderApprovalRatio` + `application.yml` 注入，OrderServiceImpl 构造注入引用、删硬编码常量。
  ③ **复测**：`mvn compile` BUILD SUCCESS + 逆向校验 **0 违规（Blocker 0/Major 0/Minor 0），关键漂移 0**，远超"漂移≤2/Blocker=0"红线。
  产出 `design/report/RCR_V1_…逆向校验报告_20260626.md`（10 字段：六维汇总 + ERR-0001 检出→已整改 + 合规亮点[拓扑精确:EDGE-0023 白名单+EDGE-0026 事件解耦/契约一致/约束内建] + 遗留豁免[优惠目录属 MOD-020 桩值/内存持久层/代表性切片，均计划内非漂移]）+ `rcr-findings.json`。状态：整改完成、允许交付。compile 0 错 0 警。**v1 阶段硬节点达成**。**下一步 D16（第 4 周·★需求变更闭环启动）**：从 D10 缺陷择 DEF-001(退费均价口径)/DEF-004(转班单价口径) 写 CR → CIA 九维影响分析 → ADR-005。
- 2026-06-26 · **重大范围调整（教师新要求）**：教师要求最终交付**完整可运行系统**。原计划（代表性切片 + 方法论）无法满足。经与用户决策：**B 偏向（核心收费闭环端到端可运行）+ 保留扩展到 A 的可能；走正式 CR 变更闭环（机制 i）降级 SRS 资源型需求；全程留痕供答辩**。含发票/对账 · H2 嵌入式 · 轻量静态演示页 · 5 项打 1 个 CR 批次。实施稿 `design/change/v2变更实施稿.md`（P0~P8 分阶段，P5 拆 4 个可运行小批次）。
- 2026-06-26 · **★需求变更闭环 P0~P3 完成（变更立项包，CCB 通过）**：
  P0 冻结基准——`git tag v1-baseline`(f646a0f) 作变更前锚点 + 建 `design/change/`、`design/change/evidence/`。
  P1 **CR-EDU-2026-0001**（P0 重大变更，10 字段，5 CR-ITEM：①资源 IFR 降级 Mock ②资源 NFR 降级本地等效 ③退费均价口径叠加消耗进度折损分档[DEF-001] ④转班单价随赠课模式对齐[DEF-004] ⑤H2+MyBatis 持久化+补核心闭环模块）。
  P2 **CIA-EDU-2026-0001**（11 字段+九维全覆盖，结论：变更可控，架构零漂移/契约向前兼容/数据无迁移；主风险=持久层回归+口径正确，均有分批次/单测/快照/回滚预案）。
  P3 **ADR-005**（迭代变更决策，11 段，摘抄被改原规则确认 ADR-001~004 不变，三方案[临时兼容/基线迭代优化/整体重构]选迭代优化，已接受）。
  CCB 评审通过（`design/change/CCB_CR-EDU-2026-0001_变更立项评审.md`）：CR/CIA 正式生效、ADR-005 已接受；硬约束 B 偏向控范围。compile 0 错 0 警。**下一步 P4**：OAS v1→v2（+x-impl-mode）+ TLCD/MDS/DTS V1.1 + diff 文档。
- 2026-06-26 · **★需求变更闭环 P4 完成（契约/约束更新）**：**OAS-EDU-V2**（`design/oas/OAS_V2_…yaml`，OpenAPI 3.0.3 合法，**16 端点**：v1 的 13 个全标 `x-impl-mode`[real 14/mock 2] + 退费 RefundCalcVO 加 refund_loss_rate 折损率字段(DEF-001) + 转班单价口径随赠课模式(DEF-004) + 补班级/排课/课消 3 端点；v1 并存可对比）+ **OAS v1↔v2 diff**（`design/change/OAS_v1_v2_diff.md`，向前兼容）+ **约束基线 V1.1 变更说明**（`design/change/约束基线v1.1变更说明.md`：TLCD 新增 C-CODE-0015 MyBatis/C-MOD-0013 Mock适配/C-CODE-0016 口径配置注入；MDS 加 status[implemented/stub/planned]；DTS 白名单不变）。compile 0 错 0 警、YAML 合法。**下一步 P5**：v2 代码 4 个可运行小批次（P5a 持久化底座→P5b 口径变更→P5c 补核心模块→P5d Mock+前端）。

---

## 11. 红线与高频提醒

- **契约/基线先行**：先定基线再写契约，先决策(ADR)再生成代码，禁止代码先行后补文档。
- **不可变性**：raw/notes 与 baselines 写入不可改；变更只新建基线，旧基线永久保留。
- **ID 终身唯一**：所有需求/模块/接口 ID 不复用、不删除，废弃仅标状态。
- **AI 要负面约束**：给 AI 的是「不能做什么」(C-ARCH/C-MOD/C-CODE)，否则它会把训练数据里的习惯模式（如在 OrderService 里发短信）写进来。
- **变更闭环不可缺环**：CR→CIA→约束/契约更新→代码变更→CRR→ADR-005→新基线，缺一即掉档。
- **缓存命中率**：约束提示词「固定前缀(全局+代码风格) + 变动后缀(模块+接口)」，统一措辞可把命中率从 ~62% 提到 ~76%。
- **批判性反思**：期末 ≥2000 字反思是评分核心之一，平时就记「哪个环节 AI 超预期/不如预期、换工具是否仍成立」。
