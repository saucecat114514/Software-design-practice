# 基线说明 · BL-20260629-02（v2 迭代基线）

## 一、基线概要
- 基线编号：**BL-20260629-02**（第 2 个基线，v2 需求变更迭代）
- 前序基线：BL-20260623-01（v1 初始基线，**永久保留、不可变、不删除**）
- 变更批次：CR-EDU-2026-0001（P0 重大变更，5 CR-ITEM）
- 冻结时间：2026-06-26（CCB 评审通过）
- 冻结快照：git tag **`v2-baseline`**（与 `v1-baseline`@f646a0f 对应，构成 v1↔v2 完整对比锚点）
- 状态：正式基线

## 二、本次变更（相对 v1）
1. 资源型 IFR 降级为本地 Mock（支付/电票/短信/小程序，契约不变）。
2. 资源型 NFR 降级为本地可运行等效（云/并发/灾备）。
3. 退费均价口径叠加消耗进度折损分档（DEF-001）。
4. 转班单价随赠课模式对齐（DEF-004）。
5. H2+MyBatis 持久化落地 + 补核心闭环 10 模块，系统完整可运行。

## 三、冻结产物清单
| 类别 | 产物 | 位置 / 冻结方式 |
|---|---|---|
| 变更三剑客 | CR-EDU-2026-0001 / CIA / CRR | 本目录 `change/`（已复制冻结） |
| 变更决策 | ADR-005 + CCB 立项评审 | 本目录 `change/` |
| 需求溯源 | RTM v2 变更增量 | 本目录 `RTM_BL-20260629-02_…变更增量.md`（不变项继承 BL-01） |
| 架构/约束/契约 | ADR-001~004、ASD、MDS/DTS/TLCD V1.1、OAS-EDU-V2 | git tag `v2-baseline` 快照（工作层 `wiki/summaries/design/`） |
| 源代码 | edu-charge-system v2（88 类，完整可运行） | git tag `v2-baseline` 快照（`code/edu-charge-system/`） |
| 回归证据 | P5a~P5d 实跑证据、rcr-findings.json | git tag `v2-baseline` 快照（`design/change/evidence/`、`design/report/`） |

> 归档约定：本实训以 **git tag `v2-baseline` + 本基线目录的变更三剑客/RTM** 共同构成 v2 基线的不可变冻结；大体量设计产物与代码以 tag 快照为准（避免重复拷贝），目录内固化变更核心凭证。

## 四、铁律
1. BL-20260623-01 与 BL-20260629-02 均**写入后不可改、不可删**；后续变更只新建 BL-…-03。
2. 任何需求/设计/代码变更须走 CR→CIA→ADR→CRR→新基线闭环。
3. 编号终身唯一、不复用。

## 五、闭环结论
CRR-EDU-2026-0001 校验通过：五项变更全落地、CIA 风险全闭环、**0 回归缺陷、0 架构漂移**、系统完整可运行（11 步实跑全 0000）。**v2 需求变更闭环完整闭合**。
