# CRR-EDU-2026-0001：CR-EDU-2026-0001 变更回归校验报告

## 1 基础元数据
| 字段 | 值 |
|---|---|
| CRR 编号 | CRR-EDU-2026-0001 |
| 关联 CR / CIA | CR-EDU-2026-0001 / CIA-EDU-2026-0001 |
| 变更等级 | P0 重大变更（全量九维深度回归） |
| 校验版本 | v2（对比基准 git tag `v1-baseline`@f646a0f） |
| 校验时间 | 2026-06-26 |
| 执行人/机器 | CodeGraph 逆向校验 + 本地实跑（11 步剧本）+ CCB 复核 |
| 报告状态 | **校验通过** |

## 2 变更溯源概述
CR-EDU-2026-0001 五项：①资源 IFR 降级 Mock ②资源 NFR 降级本地等效 ③退费均价口径叠加消耗折损分档(DEF-001) ④转班单价随赠课模式(DEF-004) ⑤H2+MyBatis 持久化+补核心闭环。CIA 预判：变更可控、架构零漂移、契约向前兼容、数据无迁移；主风险=持久层回归+口径正确。回归边界 = 此五项 + 受影响 10 模块。

## 3 校验基线依据（迭代后最新）
OAS-EDU-V2、TLCD/MDS/DTS V1.1、ASD-EDU-V1、ADR-001~005、RTM v2(BL-…-02)。逆向校验脚本对标 OAS_V2。

## 4 回归校验范围清单
- 变更点：退费 calc(折损)/转班 fee(单价)、支付回调/通知 Mock、内存→MyBatis、10 模块 15 端点。
- 关联回归：既有退费/转班/收入确认改 MyBatis 后重测；完整 11 步剧本。
- 风险复核：CIA 四条风险逐项。

## 5 九维全域回归校验结果
| 维度 | 对标 | 结果 |
|---|---|---|
| 需求(RTM/CR) | 五项变更全落地，业务规则与 CR 一致，无超范围 | ✔ |
| 架构(ASD/TLCD) | 四层单向不变，无新增漂移；技术栈新增 H2/MyBatis 经 ADR-005 | ✔ |
| 模块(MDS) | 职责/归属不变；payment 不写 crm_order，边界守住 | ✔ |
| 拓扑(DTS) | 依赖白名单内（EDGE-0002 业务→网关、EDGE-0022 课消→收入事件）；无环/反向 | ✔ |
| 契约(OAS v2) | 15 端点路径/响应/字段与 OAS v2 一致，统一 ApiResponse | ✔ |
| 代码(TLCD) | 目录分层/命名/异常/事务仅 Service/阈值配置注入合规 | ✔ |
| 数据 | 18 表 schema.sql 建、data.sql 种子；全新库无迁移；退费快照冻结隔离新旧 | ✔ |
| 业务兼容 | 退费/转班金额按新口径（预期不兼容，旧单按旧快照）；其余向前兼容 | ✔ |
| 风险预案(CIA) | 见 §6 | ✔ |

## 6 CIA 风险逐项复核
| 风险 | 等级 | 复核结论 |
|---|---|---|
| 持久层重构回归 | 高 | **闭环**：P5a 单独成批次落地，实跑退费 9500/转班 1100 + 逆向校验 0 漂移 |
| 口径变更金额错误 | 中 | **闭环**：P5b 实跑前后算例（退费高消耗 4000→3200、转班 carry 245→196）符合新口径 |
| 范围蔓延 | 中 | **已控**：B 偏向硬约束，核心闭环先 100% 跑通；扩展模块标 planned |
| Mock 语义偏差 | 低 | **接受**：OAS x-impl-mode:mock 显式声明，文档明示 |
> 无风险溢出、无新增未知风险。

## 7 回归缺陷问题清单
**0 条**回归缺陷（逆向校验 Blocker 0/Major 0/Minor 0，关键漂移 0）。

## 8 缺陷整改与复测
无回归缺陷，无需整改。（P5b 阶段曾检出 OrderServiceImpl 硬编码 Minor 已于 D15 前整改，本次未复现。）

## 9 基线一致性校验结论
代码 ↔ OAS v2 / MDS / DTS / TLCD V1.1 **完全一致**：88 类逆向图谱与正向白名单比对 0 非法依赖、0 穿透、0 越界、0 环；15 端点全命中契约。**无架构漂移、无私自改动**。

## 10 遗留问题与豁免说明
| 项 | 处置（CCB 接受） |
|---|---|
| 其余 ~11 业务模块未实现 | 计划内代表性切片（B 偏向），台账登记 status=planned，非缺陷 |
| 持久层演示用内存→已升级 MyBatis+H2 | 已落地真实持久化，原内存桩移除 |
| 外部依赖 Mock | x-impl-mode 声明，未来接真实第三方走变更流程 |

## 11 整体校验结论
**完全合规可闭环**。五项变更全部正确落地、CIA 风险全闭环、零回归缺陷、零架构漂移、基线一致、系统完整可运行（11 步实跑全 0000）。

## 12 评审归档结论
CCB 评审：变更质量达标，**允许冻结新基线 BL-…-02**；CR 置「已闭环」、CIA 置「已闭环」、CRR「校验通过」。变更三剑客闭环包（CR+CIA+CRR）成套归档。

> 运行证据：design/change/evidence/P5a~P5c2、P5d 各 evidence。逆向校验：design/report/rcr-findings.json。
