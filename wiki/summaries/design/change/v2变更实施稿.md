# v2 变更实施稿（可运行核心闭环 + 资源降级）

> 变更批次：CR-EDU-2026-0001　目标：核心收费闭环端到端可运行 + SRS 资源型需求正式降级。
> 决策：含发票/对账 · H2 嵌入式 · 轻量静态演示页 · 5 项打 1 个 CR 批次。
> 取向：**B 偏向**（核心闭环先 100% 跑通），**保留扩展到 A**（其余模块台账/桩保留，增量可补）。
> 变更前锚点：git tag `v1-baseline`（f646a0f）。

## 三条原则
1. **契约不变、实现降级**：架构(ADR-001)/模块(MDS)/接口(OAS) 基本不动，外部依赖换 Mock、基础设施换嵌入式。
2. **一切走变更闭环**：不私改 BL-20260623-01，经 CR→CIA→ADR-005→新基线 BL-…-02。
3. **全程留痕**：v1 tag 冻结、v2 增量、`git diff` 即痕迹；旧文件只增不删、v1/v2 并存。

## 目标系统形态
- 端到端闭环：登录→报名算价→下单→支付回调入账→开发票→排课/班级→签到扣课时→消课/收入确认→**退费(★新口径)**/**转班(★新口径)**→日终对账。
- 实现模块 ~12；持久层 H2 嵌入式 + MyBatis（schema.sql/data.sql）；外部 Mock 适配；静态演示页（resources/static）。

## 阶段计划（每阶段＝一个可审查/可提交里程碑）
| 阶段 | 内容 | 关键产物 |
|---|---|---|
| P0 冻结基准 | tag + change 目录 + 本实施稿 | `v1-baseline`、`design/change/` |
| P1 CR | CR-EDU-2026-0001（5 ITEM） | `change/CR-EDU-2026-0001_*.md` |
| P2 CIA | 九维影响分析 | `change/CIA-EDU-2026-0001_*.md` |
| P3 ADR-005 | 变更架构决策 | `adr/ADR-005_*.md` |
| P4 契约/约束更新 | OAS v1→v2(+x-impl-mode)、TLCD/DTS/MDS 递增、diff | `oas/OAS_V2_*.yaml`、`change/OAS_v1_v2_diff.md` |
| P5 v2 代码 | P5a 持久化底座 / P5b 口径变更★ / P5c 补核心模块 / P5d Mock+前端 | `code/edu-charge-system` 增量 |
| P6 CRR+新基线 | CRR 回归 + 实跑证据 + BL-…-02 + RTM v2 + 工作流2 | `report/CRR-*.md`、`baselines/BL-…-02/`、`automation/工作流2.json` |
| P7 子技能+资产包 | ≥5 SKILL + 五层资产包 + 扩展路线图 | `archive/` |
| P8 综合文档+演示 | ≥2000字反思 + 视频 + 变更痕迹索引 | 综合文档/视频 |

## P5 四个可运行小批次（每批次结束都能编译+启动+独立 commit）
| 批次 | 任务 | 验收 |
|---|---|---|
| P5a 持久化底座 | H2+MyBatis 依赖、schema.sql/data.sql、现有 4 模块 Repo 改 MyBatis | 启动建 ~18 表、数据落 H2、Console 可见 |
| P5b 口径变更★ | 退费叠加消耗进度折损分档(DEF-001)、转班单价随赠课模式对齐(DEF-004) | `git diff v1-baseline` 显示口径变更、算例前后值符合新口径 |
| P5c 补核心模块 | 下单/收款/回调/对账/发票/排课/班级/考勤/消课 + 审批/RBAC/通知 四层+MyBatis | 端到端剧本 9 步逐接口 200 且落表 |
| P5d Mock+前端 | 支付/电票/短信 Mock 适配 + static/index.html 演示页 | 演示页点一遍跑通、Mock 回调驱动状态流转 |

## 端到端验收剧本（"可运行"硬证据）
1.登录拿token 2.算价 3.下单 4.支付回调入账 5.开发票(≤5000自动) 6.建班/排课 7.签到扣课时 8.消课→收入确认 9.退费(新口径)算→审批→红冲 / 转班(新口径)算差价 10.日终对账。每步：接口 200 + H2 对应表新增/变更行。

## 留痕清单（答辩用）
`v1-baseline` tag｜`design/change/`(CR/CIA/CRR/diff/evidence)｜OAS v1&v2 并存｜v1 代码 tag & v2 HEAD｜BL-…-01 & -02 双基线｜每阶段独立 commit(不 squash/force-push)｜RCR(v1)↔CRR(v2) 对比｜启动截图日志｜《变更痕迹索引》｜n8n 工作流2。

## 总验收（期末优秀线）
★变更闭环完整✔｜代码可编译**可运行**+漂移≤2✔｜工程产物完整✔｜子技能≥5✔｜反思≥2000字✔｜红线全避✔。

## 执行期默认项
持久层=MyBatis｜登录=简化 token 桩(演示 RBAC)｜演示页=单页原生 JS 随 Spring Boot 启动。
