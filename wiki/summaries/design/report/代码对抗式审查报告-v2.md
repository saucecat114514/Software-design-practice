# v2 代码对抗式审查报告（下一次需求变更前的地基体检）

> 目的：在下一次需求变更前，对 v2 代码做**对抗式审查**（主动找"会崩/会算错"的地方，而非确认能跑）。
> 范围：`code/edu-charge-system` v2（88 类）。重点：业务逻辑、跨模块调用、边界/空值、事务、MyBatis。
> 方法：精读 11 个 ServiceImpl + Mapper + 事件/配置，逐一假设失败路径；逆向校验 + 实跑复验。

## 一、发现与处置

| 编号 | 等级 | 位置 | 问题 | 处置 |
|---|---|---|---|---|
| **F1** | **Major（潜在崩溃+语义错误）** | TransferServiceImpl 原路退款分支 | 转班 diff<0 且 original_route 时调用 `refundService.calc(orderId)`：① 退费按 `fin_order_snapshot` 查单，转班订单不在该表→抛"退费订单不存在"使转班崩溃；② `refund.calc` 算整单退课全额，而转班只该退**差价**，金额语义错。该路径因种子 diff>0 未被触发，是潜在雷。 | **已修**：移除该脆且错的直接调用；负差价(diffAmount<0)即应退款额、转班单状态置 `REFUND_DUE`，由财务下游按既定退款流处理。转班自身保持正确健壮，去掉对退费读模型的隐性耦合。 |
| **F2** | Minor | TransferServiceImpl | 同上语义：转班差价退款 ≠ 整单退费 | 随 F1 一并消除（不再调 refund.calc） |
| **F3** | Minor | AttendanceService.checkin | 课时账户余额为 0 时仍上报 `hour_deducted=1`，与账户实际不符 | **已修**：余额>0 才扣 1，否则扣 0，账户与上报一致 |
| F4 | Minor（接受） | PaymentService.reconcileList | `total=list.size()`，未做 SQL 真分页 | 接受：演示数据量小；真分页留待规模化（NFR 降级范围内） |
| F5 | Info（接受） | consume→revenue / refund→revenue 事件 | Spring `@EventListener` 默认同步、同事务，"弱一致异步"表述偏强 | 接受：代码层已解耦（消费者不依赖生产者 Service）；监听器仅记录、抛错会回滚发布方事务但当前不抛，安全。真异步可后续 `@Async`/`@TransactionalEventListener` |
| F6 | Info（接受） | GlobalExceptionHandler | 业务异常统一返回 HTTP 200 + 错误码（非 4xx/409） | 接受：C-CODE-0013 统一响应体设计（错误以 code 区分）；OAS 的 4xx 声明为约定式，不影响功能 |
| F7 | Info（接受） | 时间戳类 ID（ORD-/LSN-/CSM- 等用 currentTimeMillis） | 同毫秒并发可能撞键（MERGE 覆盖） | 接受：单机演示单线程；规模化改雪花/序列（NFR 降级范围内） |

## 二、修复清单
1. **F1**：`TransferServiceImpl` 删除 `RefundService` 依赖与 `refund.calc` 调用、相关 import 与构造参数；负差价标记 `REFUND_DUE` 交财务下游；更新类注释。
2. **F3**：`AttendanceService` 扣课时改为 `余额>0 ? 1 : 0`。

> 设计说明：DTS EDGE-0023（转班→退费）由"直接全额退费调用"调整为"转班记录退款额、财务下游处理"，避免跨模块读模型耦合与口径误算；属实现层健壮性优化，模块边界/拓扑白名单不变（逆向校验 0 漂移）。

## 三、修复后复验
- `mvn compile` BUILD SUCCESS。
- 实跑回归：转班 void=245/diff1100、carry=196/diff2080（口径不变）；签到 present/扣1/余39（正确）；退费高消耗 3200/折损0.2（不变）——**无回归**。
- 逆向校验：**88 类，违规 0（Blocker/Major/Minor 全 0），关键漂移 0**。

## 四、结论
v2 代码经对抗式审查：**1 个 Major 潜在缺陷（转班→退费崩溃/误算）已修，1 个 Minor（签到扣课时）已修**，其余为可接受的演示期取舍（已逐条记录）。当前版本**编译通过、11 步闭环可运行、0 架构漂移、关键失败路径已加固**，可作为下一次需求变更的稳固地基。
